[project]
name = "ai-log-explainer-backend"
version = "0.1.0"
description = "Backend for AI Log Explainer"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "httpx2>=2.9.1",
    "pydantic>=2.13.4",
    "uvicorn>=0.52.1",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
